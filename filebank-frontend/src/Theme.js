// Define your color palette, sizes, etc.

const colors = {
  primary: '#1E90FF', // Your lucky Rat blue
  accent: '#FFD700',  // Gold
  secondary: '#32CD32', // Green
  background: '#F0F8FF', // Light blue
  danger: '#FF4D4F',
  success: '#52C41A',
  warning: '#FAAD14',
  textPrimary: '#333',
  textSecondary: '#888',
};

const spacing = {
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

const borderRadius = {
  sm: 4,
  md: 8,
  lg: 12,
};

const theme = {
  colors,
  spacing,
  borderRadius,
};

export default theme;

